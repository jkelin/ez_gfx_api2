# TODO

## P0

- Lower compiled transient alias assignments into Vulkan, DX12, and Metal resource placement, including alias barriers and overlap-safe retirement (P-004, P-008, P-009).
- Add managed render-target creation, format-capability probing, per-target clears, sampled/storage bindings, resize/history, graph attachment, and lifecycle APIs across Rust/FFI/backends (P-008, P-009, P-010, P-015). Native single-mip sampled creation/probing plus safe create/query/destroy lifecycle are implemented on Vulkan/DX12/Metal; pass attachment with per-target clears is implemented on Vulkan/DX12 with serial-only evidence, not full proof — both native suites stall under default parallelism on RTX 3080 (Vulkan: 9 GPU tests, 0 GPU completions in >12min; contained via serial pin, revisit on hardware/driver change); interim evidence procedure is serial `--test-threads=1`, not the proof standard (Metal is code-reviewed only, Mac run pending); managed FFI creation/queries/destroy/probe/begin is implemented (ABI 25, serial Vulkan+DX12 evidence); heap slots unify textures and render targets in one registry allocator with serial collision/exhaustion/reuse evidence; MSAA resolve is implemented (multisampled allocation + per-backend resolve with serial Vulkan+DX12 clear-to-readback evidence, Metal eye-reviewed Mac-pending; multisampled draws stay Unsupported pending pipeline sample counts).

## P1

- Publish an API parity matrix and add missing render-target, screenshot-save, and graph-authoring interfaces with ABI tests (P-002, P-010, P-019).
- Add one immutable shared `FrameBeginConfig` to `frame_begin`, `begin_render`, and `begin_render_target` (P-013). `wait_for_geometry_uploads` defaults to `true` and covers both named vertex heaps and the global index heap; `false` transfers geometry visibility scheduling to `DeviceReady` consumers. `TextureMipWait` defaults to `Coarsest`; `None` disables frame submission texture-upload waits, while `ThroughLevel(level)` requires readiness through a selected minimum mip, where mip 0 is finest and higher numeric levels are progressively coarser/uploaded first. With `None`, applications gate first texture visibility through `DeviceReady` or a fallback and finer residency through `set_texture_residency`/`texture_residency`; polling, completion-gated descriptor publication, recycling, progressive uploads, and loss handling continue. Apply enabled reachable upload readiness once per frame as aggregate transfer-domain prefixes outside graph node/resource scheduling; never attach asset-upload GPU waits to particular textures, allocations, draws, or graph nodes. The safe Rust interface owns policy/state validation; the C ABI validates enum tags, booleans, reserved fields, mip values, layouts, and defaults before delegation. Acceptance includes default preservation, manual geometry and texture scheduling, mixed mip chains, uploads admitted during recording, failed/unreachable submissions, descriptor-publication delay, device loss, all three begin paths, Rust/C parity and documentation, and Vulkan/DX12/Metal behavior proving at most one frame-level prefix per enabled active transfer domain and no asset-specific graph wait actions.
- Add the selected mapped staging lease interface for procedural vertex/index writes (P-011). Typed heap/allocation handles, generation/owner validation, reflection lookup, lossless upload events, and completion-gated transient structured/indirect recycling are implemented; slice uploads still copy caller bytes into mapped staging. Implementation adds a frame-bounded mapped lease that callers write directly and then commit or cancel exactly once, bound to the `ContextIdentity` epoch so use-after-loss fails instead of silently copying. Acceptance is procedural-upload pixel parity with the copy path, commit/cancel/failure tests, and no cross-frame lease stalls. Evidence: ABI 30 Linux remote 73/73, macOS remote 51/51, and local Windows HAL/Vulkan/DX12/safe-facade matrix 94/94 passed.
- Retire removed vertex/index ranges against fine-grained graphics completion instead of full context idle (P-011). Removal currently waits for native idle before returning a range to the free list. Implementation quarantines each removed range against its frame's graphics completion token and returns it to the free list once that token completes, while stale, foreign, and double frees stay rejected. Acceptance is repeated remove/upload cycling with no `wait_idle` plus coalescing and generation-identity tests.
- Expose validated per-draw/per-pipeline viewport and scissor state and batch consecutive equal-state MDI ranges; current backends set only full-render-area state (P-016).
- Expose deterministic adapter enumeration and explicit stable adapter selection through Rust (implemented: per-backend `enumerate_adapters`, `ContextOptions::with_adapter`, catalog-routed admission, `query_adapter_report` diagnostics on Vulkan/DX12 with headless+GPU tests; Metal implemented eye-review-only, Mac run pending) and FFI (implemented: ABI-26 adapter selectors on both context descriptors plus enumerate query pair with rejection diagnostics; Metal Apple-gated eye-review only, Mac run pending), including admitted limits/formats and rejection diagnostics (P-003, P-022).
- Add `raw-window-handle` Rust adapters, Linux X11/Wayland Vulkan surfaces, DPI-aware recreation coverage, and native presentation tests (P-017, P-022). Headless Vulkan (ABI-27 platform 3, no Win32 ext) now supports logical surfaceless initialization when an ICD lacks `VK_EXT_headless_surface`; native initialization is proven on NVIDIA 580.126.09 and Lavapipe 25.2.8, so hosted Lavapipe executes `native_init`. Real headless presentation still requires the extension: Linux pixel coverage asserts only `Unsupported` as the capability result and keeps other errors terminal; native target-only render-target, clear/readback, and MSAA tests remain hardware-proven.
- Add bounded, validated host-owned pipeline-cache import/export envelopes with backend/device/driver/schema compatibility; current caches are process-local only (P-007, P-024).
- Finish terminal device-loss behavior for staging leases and waits. Pending texture decode/transfer uploads emit terminal loss events and transfer workers retain sticky loss; loader-lock teardown remains abandon-only (P-023, P-026).
- Add correlation IDs, sequence/domain, clocks, units, payloads, and cleanup outcomes to the lossless typed upload queue. The queue currently preserves every texture/vertex/index ownership, readiness, cancellation, and failure transition; bounded diagnostics remain separate (P-023, P-026, P-028).
- Design a dedicated transition/acquire queue per Vulkan/DX12 transfer worker to remove remaining host waits (P-012, P-015). Metal nonblocking submission is implemented. Implementation moves acquire/transition barrier handoff off the calling thread onto the worker-owned queue so the submit path never blocks on transfer progress, keeping barrier ordering and error/loss propagation identical. Acceptance is nonblocking submit behavior under texture/vertex streaming load with pixel parity on Vulkan and DX12.
- Fix CPU mip generation dropping trailing odd rows/columns; area-weighted box filter must cover the full extent (P-014; implemented in this change with runtime tests).
- Filter sRGB mips in linear light (decode/filter/encode RGB, alpha linear unchanged) instead of encoded-space averaging (P-014; implemented in this change with runtime tests).
- Optional: raise the synchronized texture heap capacity above 1024 (`docs/textures.md` §Texture heap capacity, P-007). The cap is one contract across core admission, compiler/HAL reflection, runtime handles, Vulkan/DX12 descriptor counts, Metal argument-buffer layout, and the Slang static array length, so the change must land atomically in every layer plus device-capability admission. Acceptance is updated capacity/admission tests, ABI layout/export parity, and pixel coverage on all three backends.

## P2

- Add deterministic offscreen pixel goldens for fork/join graphs, scaled targets, storage images, history, aliasing, and all supported backends; run Vulkan, DX12, and Metal native suites on guaranteed-capability GPU runners (P-019, P-020, P-022).
- Benchmark graph compilation, pass coalescing, alias savings, event latency, async Sponza loading, GPU frame time, and package size before claiming broader selected-plan performance gains. Scoped texture staging/batching and 2048² atlas measurements are recorded in `docs/textures.md`, not full-scene performance evidence (P-007, P-008, P-009, P-012, P-018, P-028).
- Resolve the P-004 allocator-selection mismatch: `gpu-allocator` covers Vulkan/DX12, while Metal uses a backend-native allocator; either supply equivalent selected evidence and amend the decision or adopt a maintained cross-backend implementation (P-004).

## Tooling

- Add target-native release jobs that publish separate runtime/FFI, compiler/tool, and optional Basis artifacts with deterministic manifests, license/provenance records, binary-import audits, signing, and Apple notarization (P-001, P-025, P-027, P-029).
- Configure Miri and integrate it with nextest (P-019, P-029).
- Pin the native GPU suites serial in-harness (pinned, not a driver fix): `.config/nextest.toml` assigns both backend lib suites to a `gpu-serial` group (`max-threads = 1`), covering `cargo nextest` locally and CI's DX12-lib run; `.cargo/config.toml` sets `RUST_TEST_THREADS = "1"`, covering bare `cargo test` (libtest has no per-suite knob, so every libtest run repo-wide is serial). Fresh-target-dir evidence with default invocations (no thread flags): `cargo test -p ez-gfx-backend-vulkan --lib --target-dir target/pin-vk` (23/23, 2.67s test time) and `cargo test -p ez-gfx-backend-dx12 --lib --target-dir target/pin-dx` (13/13, 2.11s test time). Stall symptom on RTX 3080 Windows: default parallelism stalls with 9 Vulkan GPU tests at 0 completions in >12min; in-session repro with `--test-threads=8` shows 13 CPU tests passing while every GPU-device test sits >60s (timeout EXIT=124), whereas `--test-threads=4` on the 9 GPU tests passes in 1.38s. Per-test QueueGate/Observer/temp-file sharing is ruled out by the green 2-/4-thread runs; prime suspect is concurrent per-test VkDevice/ID3D12Device creation wedging the driver at >=8 concurrent creations. Revisit only if runner hardware/driver changes or the suites are restructured to share devices.
- Linux remote multi-ICD enumeration is unstable in-process (not a driver fix yet): with 9 ICD manifests the full ez-gfx lib suite deterministically fails `explicit_selection_creates_context_for_enumerated_adapter` (report enumeration sees the NVIDIA adapter, the immediately following create enumeration lacks it, AdapterNotFound maps to InvalidArgument), while forced-NVIDIA and forced-Lavapipe full suites are each 32/32 green and deviceUUIDs are stable across processes. Interim: optional `REMOTE_TEST_LINUX_VK_DRIVER_FILES` pins `VK_ICD_FILENAMES` for remote-test-linux. Revisit with loader/ICD isolation diagnostics before claiming multi-ICD determinism.
