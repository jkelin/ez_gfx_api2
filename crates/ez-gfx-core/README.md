# ez-gfx-core

Backend/compiler-free semantic contracts: stable resource IDs, logical binding layouts, capability profiles, handles, and validation errors. It depends on no sibling crate. HAL and runtime consume these types; compiler maps Slang reflection into them, while FFI exposes validated ABI views.